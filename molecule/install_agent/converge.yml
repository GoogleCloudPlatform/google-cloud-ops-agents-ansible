---
- name: Converge
  hosts: all
  tasks:
    - name: Include cloud_ops role
      include_role:
        name: 'stackdriver-ansible-role'
      vars:
        agent_type: "{{ lookup('env', 'AGENT_TYPE') | default('monitoring', True) }}"
        package_state: present
        version: "{{ lookup('env', 'VERSION') | default('latest', True) }}"
        testing: True
